interface:
  display_name: "memento"
  short_description: "Turn memory photos into Dreamina prompts."
  default_prompt: "Use $memento to turn my memory photos into a short Dreamina film."

policy:
  allow_implicit_invocation: true
